//- VARS


//- FUNCTIONS

//- breadcrumbs
mixin breads (items)
	//- if !items
	//- 	- items = ['Главная']
	each item, i in items
		if i < items.length-1
			li: a(href="#") #{item}
		else
			li.current: span #{item}